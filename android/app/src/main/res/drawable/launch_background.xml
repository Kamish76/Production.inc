<?xml version="1.0" encoding="utf-8"?>
<!-- Production.Inc Launch Screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color matching game theme -->
    <item android:drawable="@color/launch_background_color" />

    <!-- Company logo/game icon (when you have one) -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
    
    <!-- Optional: Loading text at bottom -->
    <!-- <item android:gravity="bottom|center_horizontal">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
        </shape>
    </item> -->
</layer-list>
